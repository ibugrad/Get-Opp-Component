public class GetOppLwc {

    @AuraEnabled(cacheable=true)
    public static list<opportunity> getOppRecords(string ownId){
        
        return [SELECT Id, Name, Stagename, Closedate, Amount WHERE Opportunity WHERE Id=: ownId];
        
    }
}
